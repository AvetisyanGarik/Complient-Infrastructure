---
# Common Variables

# Directory Paths for Configs
golden_config_path: "Configs/golden_configs"
diff_output_path: "/runner/project/Configs/diff_output/{{ lookup('pipe', 'date +%Y-%m-%d') }}"
date_tag: "{{ lookup('pipe', 'date +%Y-%m-%d_%H-%M-%S') }}"

#Email variables
smtp_host: "smtp.gmail.com"
smtp_port: 465
smtp_username: "{{ lookup('env', 'SMTP_USERNAME') }}"
smtp_password: "{{ lookup('env', 'SMTP_PASSWORD') }}"
email_to: "garik.avetisyan@udx.com"